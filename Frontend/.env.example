# Base URL of the ASP.NET Core backend API.
# In dev this isn't strictly needed since vite.config.js proxies /api,
# but it's used as a fallback / for production builds.
VITE_API_BASE_URL=http://localhost:5194
